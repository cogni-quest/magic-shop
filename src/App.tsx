import { Shop } from '@/ui/Shop'

/**
 * One screen, so there is nothing to choose between. The component exists to
 * keep `main.tsx` about mounting and this file about what gets mounted.
 */
export function App() {
  return <Shop />
}
