import { ru } from './ru'

/**
 * The text pack in use. One language today; a second one is a file beside
 * `ru.ts` and a change here, because everything already goes through `t`.
 */
export const t = ru
